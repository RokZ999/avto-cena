export interface CarDataSummary {
  max_calculated_price: number;
  min_calculated_price: number;
  diff_first_last_price: number;
  diff_first_min_price: number;
  avtoNetDistinctPrice: number;
  doberAvtoDistinctPrice: number;
}
